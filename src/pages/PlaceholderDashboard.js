// src/components/PlaceholderDashboard.js

import React from 'react';

const PlaceholderDashboard = ({ userType }) => {
    return (
        <div>
            <h1>{userType} Dashboard</h1>
            <p>This dashboard is coming soon!</p>
        </div>
    );
};

export default PlaceholderDashboard;
